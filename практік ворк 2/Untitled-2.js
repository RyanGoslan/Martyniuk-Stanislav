
const n = parseInt(prompt("Введіть число n:"));

// Перевіряємо, чи введене значення є числом
if (isNaN(n)) {
    console.log("Введено некоректне значення. Будь ласка, введіть число.");
} else 
    console.log(`Парні числа від 2 до ${n}:`);
    
    // 2. Використовуємо цикл for для виведення всіх парних чисел від 2 до n
    for (let i = 2; i <= n; i += 2) {
        console.log(i);
    }
    